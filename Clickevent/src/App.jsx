import Button from "./Button";

function App() {
  return <Button></Button>;
}
export default App;
// click events = an interaction when user click on a specific elements we can respond to clicks by passing a callback to the onclick event handler.
